# Environment Variables for Docker Compose
#
# IMPORTANT: Copy this file to .env and update with your actual values
# Command: cp .env.example .env
#
# Note: The api/.env file contains additional configuration for the .NET API
# This file is specifically for docker-compose.yml

# ============================================
# SQL Server Configuration (Docker)
# ============================================

# SQL Server SA (System Administrator) Password
# Requirements: At least 8 characters including uppercase, lowercase, digits, and symbols
MSSQL_SA_PASSWORD=YourStrong!Password123
